.venv/
__pycache__/
*.pyc
data/
.pytest_cache/
.ruff_cache/
.DS_Store
dist/
*.egg-info/
# control-plane runtime state
.pipeline-state/
.em-archive/
.measurement/
.cache/
.tpm/
.claude/

# In-the-wild catch ledger — runtime witness written by refreeze.sh record_catch
# (linked from the Blueprint); per-repo state, never committed (missing = empty).
.catch-ledger.json

# TPM delta staging — consumed by refreeze.sh, never committed as frozen state
scripts/.approved/incoming/

# secrets + notebook cache
.env
