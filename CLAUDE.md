# CLAUDE.md — Master LLM Context File

> OpenCode and Claude Code can read this file via their file tools. You are
> expected to read it at the start of every session, and to consult it again
> before any action that touches the document layer (`BLUEPRINT.md`,
> `CONVENTIONS.md`, `docs/DECISIONS.md`, this file's correction log).
> Keep it current. Every correction you make to the LLM should be recorded here
> so the mistake never happens again.

---

## Project Overview

**Name:** vortex
**What it does:** One place that owns every local LLM on this machine. Installs,
configures, and runs inference runtimes (mtplx, omlx, vmlx, llama-server, LM
Studio, ds4), owns model lifecycles, and exposes loaded models to every client
(testchat, OpenCode, pi) as one universal OpenAI Chat Completions surface.
The process is called modelmux; `vortex` is the app and repository name.
**Status:** In Development (brownfield adoption D-165 — first-run subject; prototype shipped at b76b5ea, control plane installed 2026-08-15)

---

## Tech Stack

> Adapted from template default (Rule 3) — FastAPI + SQLite app with no DB.

```
Language:     Python 3.12+ (3.14.6 in dev)
Framework:    FastAPI (uvicorn), pydantic
Database:     none (catalog = config/catalog.json, config-first)
Auth:         none — local-only daemon on 127.0.0.1
Hosting:      local macOS (Apple Silicon); Lima VM for pipeline runs
CI/CD:        (not yet connected — plan for after remote exists)
Testing:      pytest (frozen suite, 33 node-ids across 5 files, hash-pinned
              in scripts/.approved/frozen-manifest; legacy 16-test snapshot
              pinned separately in scripts/.approved/legacy-pin.json — NOT an oracle)
```

---

## Project Structure

```
vortex/
├── src/                  # application source
│   ├── api/              # route handlers
│   ├── models/           # data models
│   ├── services/         # business logic
│   └── utils/            # shared utilities
├── tests/                # TPM-authored frozen suite (INV-1); changes only via refreeze.sh
├── docs/                 # architecture, decisions, product, TPM role, escalation
├── tasks/                # EM write lane (plan.json) + session notes + backlog
│   └── CURRENT.md        # session notes — active work, halt notes (the PRD lives in scripts/.approved/)
├── scripts/
│   ├── bootstrap.sh      # one-time setup (sets core.hooksPath)
│   ├── phase-gate.sh     # lane + integrity gate (INV-2/3, frozen spec)
│   ├── orchestrate.sh    # shell-driven task-DAG conductor (owns all procedure)
│   ├── validate-plan.py  # plan.json gate (atomicity, DAG, coverage, mapping)
│   ├── refreeze.sh       # ONLY path frozen TPM artifacts change (auto-applies when all mechanical preflights are green; no human approval step, D-121)
│   ├── check-test-surface.py  # INV-4: tests ⊆ locked surface
│   ├── schemas/          # plan / diagnosis / contracts schemas
│   └── .approved/        # frozen TPM spec: PRD, ERD, contracts, VERSION, hashes
├── .opencode/
│   └── prompts/          # agent role definitions (em/coder)
├── .githooks/            # pre-commit gate for the interactive/human path
├── .gate-paths           # configurable directories for INV-2 enforcement
├── CLAUDE.md             # this file
└── CONVENTIONS.md        # code style rules
```

---

## Code Conventions

- Always use type hints on function signatures
- Prefer functions over classes unless persistent state is needed
- Use `logging` from the standard library — never `print()`
- One responsibility per function — if it needs a comment explaining what it does, split it
- Tests live in `tests/` mirroring `src/` structure (e.g. `src/services/user.py` → `tests/services/test_user.py`)
- Tests are TPM-authored and land via `scripts/refreeze.sh` BEFORE the code they gate — never written after the fact to match an implementation
- Use `pydantic` for data validation and serialization
- Environment variables via `python-dotenv` — never hardcode secrets

---

## What NOT To Do

> These are guardrails. Do not override them without explicit human instruction.

**Code guardrails:**
- **Do not add dependencies** without asking first
- **Do not refactor files** unrelated to the current task
- **Do not change the database schema** without explicit instruction
- **Do not remove error handling** to simplify code
- **Do not use `Any` type** — be specific
- **Do not write `TODO` comments** — either implement it or raise it as a task
- **Do not use `time.sleep()`** in production code — use proper async patterns
- **Do not commit secrets** — use `.env` and ensure `.gitignore` covers it

**Pipeline guardrails (Rules 6-7, see BLUEPRINT.md; ladder details in DECISIONS.md D-26..D-32):**
- **No agent authors or edits tests** — the suite is TPM-authored, installed only via `scripts/refreeze.sh`, and hash-pinned in `scripts/.approved/frozen-manifest` (INV-1, now structural: tests are written before the code exists, by a tier that never sees the implementation).
- **Do not cross role boundaries** — Coder writes exactly the one file its task names (`phase-gate.sh task`); EM writes `tasks/` only (`phase-gate.sh em`). Post-D-53 enforcement is shell-owned: the model reply never touches the filesystem — the shell parses it and writes to exactly the task's declared path. `scripts/phase-gate.sh` re-verifies the working tree after every phase and fails closed on any out-of-lane change (INV-2).
- **Tests observe only the locked surface** — imports from `contracts.entry_points`, routes from `contracts.routes` (INV-4, checked at freeze time by `scripts/check-test-surface.py`).
- **Do not skip escalation** — retry → EM consult → brief/plan revision (bounded) → batched TPM bundle → re-freeze (gate-approved: refreeze auto-applies on green preflights, D-121). All counters shell-owned. See `docs/ESCALATION.md`.
- **Sync the stack before every freeze (D-50).** The TPM chooses the tech stack at spec time. Before running `refreeze.sh`, check the staged tests' imports against `requirements.txt` and add anything missing (this edit is in the conductor's lane). The sandbox image rebuilds itself when `requirements.txt` or `Containerfile` change — never manually delete or rebuild images.
- **TPM shuttle is a verbatim relay (D-49).** When the CEO asks for the TPM prompt/briefing: run `scripts/tpm-pack.sh` and reproduce its ENTIRE stdout in your reply, unabridged — never summarize it, never point the CEO at repo files (the bundle is assembled from several sources; it cannot be hand-collected), never claim it is "in the clipboard." When the CEO pastes a TPM reply back: write it to a temp file unmodified and run `scripts/tpm-unpack.sh <file>` — do not re-type or edit it.
- **TPM/milestone runs are inform-first (D-139).** Diagnosing a fix as needing a TPM round-trip or a milestone (orchestrate) run is NOT a launch: stop and ask the CEO — including who will take the TPM seat. The seat may be a web-chat model, a `scripts/tpm-agent.sh` agent, or the same LLM already on the job, by the CEO's assignment; never assume "the TPM is someone else" or that the run proceeds on an agent's judgment alone.

**Operating guardrails (from hard-won failures — see BLUEPRINT.md):**
- **Do not set a thinking model as the active model.** Thinking models leave `content` empty and put output in `reasoning_content`, which breaks parsing. The model must be non-thinking local OR frontier.
- **CARDINAL RULE — an EM/coder failure is never re-run blind.** If an EM or coder call fails, stop and troubleshoot the root cause — read the failure message, fix the harness/context/spec — never re-run the same call expecting the model to succeed next time. One attempt per run per call; a re-run is legitimate ONLY after a root-cause fix (then exactly one clean run). Measure and test through the pipeline's own machinery (`llm-call.sh` + schema + profile budget), never a hand-rolled copy of it.
- **Do not trust your own "it works" — only passing tests confirm success.** Run `pytest`. The tests are binding automated completion evidence, not your assessment. Do not mark a task done on self-judgment.
- **Do not proceed past an unreachable LM Studio or a missing service** — halt and report.
- **Do not invent product or architecture decisions to fill an ambiguous spec** — that is the human's job. Halt and ask.
- **Do not run destructive commands** (`rm -rf`, `git push --force`, drop tables, delete files outside the project) — halt and ask.

---

## Current Focus

The frozen spec (PRD/ERD/contracts + version) lives in `scripts/.approved/`;
`tasks/CURRENT.md` holds session notes and halt notes. New features start in
the TPM web chat (see `docs/TPM-ROLE.md`) and enter via `scripts/refreeze.sh`.

---

## Capability Ladder (D-27)

| Tier | Where it runs | Produces | Writes |
|------|---------------|----------|--------|
| **CEO** (human) | conversation with the conductor | business intent | — (runs no commands, D-40) |
| **TPM** (CEO-assigned seat, D-139) | web chat (D-38), scoped repo agent via `scripts/tpm-agent.sh` (D-39), or the same LLM already on the job — the CEO names the holder per session | PRD, ERD + `contracts.json`, the test suite | nothing directly — installed via `scripts/refreeze.sh` (auto-applies on green preflights, D-121), frozen in `scripts/.approved/` + `tests/` |
| **Conductor** | any chat agent the CEO chooses (Claude Code, OpenCode's Build, or a plain shell) | status reports, script invocations | docs/session notes; denied on `tests/`, `scripts/`, `src/`, control plane (D-40) |
| **EM** (mid-tier LLM) | one HTTP completion via `scripts/llm-call.sh` (D-53) | `tasks/plan.json` (decomposition), `tasks/diagnosis.json` (consults) — the shell writes both, not the model | `tasks/**` only |
| **Coder** (local LLM) | one HTTP completion via `scripts/llm-call.sh` (D-53) | one file per task, sentinel-wrapped in the reply | that one file only (gate-enforced) |

Which actual model backs EM/coder is never recorded in this repo: the CEO
maps roles to models in `~/.config/sw-dev-blueprint/models.env` (D-53,
succeeding D-41's `opencode.json` mapping). The blueprint constrains model
*class* only (frontier / mid / local non-thinking), never model identity.
No mapping for a role is a hard halt, never a silent substitution.

Tests are **run by the shell** (`pytest --json-report`, parsed by
`scripts/orchestrate.sh`) — there is no test agent. The shell orchestrator is
the only actor with procedural authority: it validates the plan, walks the
DAG, runs gates and acceptance, owns all state and escalation counters
(D-26). The EM advises at exactly two shell-initiated points; it never drives.
Neither EM nor coder has any tool or filesystem access — the orchestrator
gathers whatever context a call needs into the prompt and writes the reply to
disk itself (D-53); there is no agent harness in the execution loop at all,
only in the CEO-facing conductor seat, which never touches trusted state.

**The loop (all steps conductor-driven; the CEO only talks):**
TPM spec frozen (`refreeze.sh` — applies automatically when every
mechanical preflight is green, D-95/D-121; `--diff` shows a read-only
preview) → `scripts/orchestrate.sh` → EM emits plan → validated → coder executes
one task at a time → mapped frozen tests + gate after each →
delta-mapped verdict green = done (D-112; the full frozen suite is an
on-demand `--full-suite` regression check). Failures climb the escalation ladder (`docs/ESCALATION.md`);
spec problems come back as a batched bundle for the TPM seat and
re-enter via `refreeze.sh`.

---

## Reporting

When summarizing work since the last PM review (status reports, commit scoping, progress updates):

1. Read `docs/.pm-last-review` to get the last reviewed ref:
   ```
   LAST=$(cat docs/.pm-last-review 2>/dev/null || git rev-list --max-parents=0 HEAD)
   ```
2. Derive the commit list from the tree, not memory:
   ```
   git log "$LAST"..HEAD --oneline
   ```
3. State the scope explicitly in the report: "N new commits since reviewed ref `$LAST`".
4. Never write or advance `docs/.pm-last-review` — PM-owned.
5. If the file is missing (fresh checkout), the `git rev-list` fallback uses the initial commit — the scope becomes the entire history, which is correct for a first report.

---

## Operating Rules

> A rule that cannot be enforced mechanically is a suggestion, not a rule. Document the enforcement mechanism alongside every rule — and where there is none, say so explicitly.

Seven rules for agents working in this repo, derived from failures in prior sessions. Rules 2–7 are advisory — they rely on PM review for enforcement. Rule 1 has a mechanical backstop (see footnote).

1. **Report against the tree, never memory.** Derive your commit list from `LAST=$(cat docs/.pm-last-review); git log "$LAST"..HEAD --oneline`. State the range. A report that disagrees with `git log` is a defect regardless of the underlying work. *(Mechanical backstop: `docs/.pm-last-review` + PM source-side reconciliation.)*

2. **One commit, one concern.** Any change to a gate, invariant (INV-1/INV-2), permission, or model choice gets its own isolated commit whose message names it as such. Never bundle a constraint change with unrelated edits.

3. **A change to what a rule does is stop-and-ask.** Improving how a gate detects — fix freely. Changing what happens on a violation, or relaxing any constraint — stop and ask the PM first, even mid-run, even if the rule is what's slowing you down. The rule slowing you down is usually it working.

4. **Conditionals are checkpoints.** "Only do X if Y fails" means: when you reach that point, report whether Y failed and what you chose. If Y didn't fail, say so — don't silently act.

5. **Read the artifact, not the summary.** Report from committed files, never from another agent's summary or your own memory of a run. When source and summary disagree, source wins.

6. **"Detected" ≠ "enforced"; "nothing went wrong" ≠ "safeguard works."** Keep standalone-test results and live-run results as separate claims. An untriggered safeguard is inconclusive, not green.

7. **Decide trivial calls; escalate only contested principles.** If the PM has stated the governing principle ("put it where process docs live"), execute — don't re-ask for confirmation or surface options for a low-stakes choice. Escalate only when the principle itself is unclear, or when correctness is genuinely at stake (then asking is correct, not a failure).

---

## Key Contacts / Roles

| Role | Name |
|------|------|
| Product owner | arc.elixir (CEO) |
| Lead dev | modelmux (the pipeline process — see Capability Ladder) |

---

## LLM Correction Log

> When the LLM makes a mistake and you correct it, log it here.
> This is the most valuable section — it prevents repeat mistakes.
> A project 6 months in should have a rich log. That means the system is working.

| Date | Mistake | Guard Added |
|------|---------|-------------|
| 2026-09-02 | The v27 "Stop Vortex" freeze (POST /api/shutdown + a header button; inventory `app.py`,`ui.py`) passed every refreeze preflight and froze clean, then the FIRST orchestrate halted at the plan gate: the standing `contracts.smoke_checks` and `no_edit_files` still pinned `manager.py` — a file OUTSIDE this milestone's inventory. `check-spec-delta` only validates those keys when they CHANGE, so a carried stale entry sails through the freeze; `validate-plan.py` enforces `smoke_checks ⊆ files` AND `no_edit_files ⊆ files`, so it fired at plan time (v28 re-freeze). Then the local coder (mtplx-qwen) burned strikes on `app.py`: with only prose guidance it reached for the low-level `_terminate_pid(pid)` helper (wrong type) instead of the `lifecycle.terminate(entry)` instance method, and left unused imports; a v29 re-freeze carrying `## Coder briefs (verbatim)` that named the exact in-scope objects/method/imports got it green (T1+T2 pass, `[success]` e3a1e93). Three freezes (v27→v28→v29) to ship. | Two guards. (1) A build freeze whose inventory DIFFERS from the prior milestone's must EXPLICITLY stage `smoke_checks` and `no_edit_files` scoped to the new inventory (empty `{}`/`[]` when the new files need neither) — never let them carry a prior milestone's file; the freeze preflights won't catch it but the plan gate will, one wasted cycle later. A compile-only smoke check is also vacuous (M35 red-check rejects it) — pin the real frozen tests via `test_mapping` instead. (2) For a local coder, author `## Coder briefs (verbatim)` from the start on any task that calls a class instance's method: name the exact object already in scope (`manager`, `lifecycle`), the exact method (`lifecycle.terminate(entry)`), and the exact imports — prose like "terminate via lifecycle.terminate" is not enough; the model grabs the nearest module-level symbol. Meta-rule: freeze-time gates and plan-time gates enforce DIFFERENT invariants — a clean freeze is necessary, not sufficient; dry-run the plan mentally (inventory ⊇ every smoke_checks/no_edit/test_mapping/route-pin reference) before freezing. |
| 2026-08-30 | The EM claimed unchanged self-owned contracts (`build_app`, `UI_PAGE`, module ids) on its first two plan attempts for the v14 milestone (spec v20, engine-wrapper inventory). The plan gate rejected both — correct behavior — but the EM could not self-correct because the rejection text names no specific ids and the EM cannot distinguish changed-vs-carried from the valid-id list alone. A v15 errata carrying a negative allow-list ("never claim these four ids") was needed to steer it past the gate. Two wasted EM calls. | The EM prompt surface does not include the `changed_contract_ids` set from the active DELTA range; the plan-gate rejection does not name the offending ids. Filed as a P2 backlog item in the blueprint (EM plan gate self-correction signal). Until one of the two structural fixes lands, a freeze that introduces new contracts while carrying self-owned ones needs an errata with an explicit exclusion list. |
| 2026-08-30 | The previous TPM session for the v14 milestone misdiagnosed a test failure and did not write correction log rows — the documentation process itself failed. This session (the one that shipped v14) also initially skipped the correction log, DECISIONS.md, and BACKLOG.md updates after the `[success]` commit. The advisory maintenance contract has no mechanical enforcement; under shipping pressure, agents optimize for "get the milestone green" and skip documentation. | Added a post-success reminder banner to `orchestrate.sh` (blueprint `429ad18`): the success path now prints a Maintenance Contract checklist (correction log, DECISIONS.md, BACKLOG.md) immediately after the "feature done" banner. Advisory only — no gate — but puts it in the agent's face at the right moment. Meta-rule: an advisory process that agents skip under pressure needs at minimum a prompt-time reminder at the moment the obligation is due. |
| 2026-08-30 | The coder session that added the 4 new wrapper specs to `discovery.py` did not update `REGISTRY_ORDER` in `tests/test_discovery.py`, did not account for `probe_version=False` on new specs breaking the existing assertion, and did not anticipate that `known_paths` would find real binaries on the host even with an empty `search_path`. Three test failures on the first commit attempt, all caught by the frozen-manifest gate (which worked as designed). | When adding entries to a registry tuple that has a parallel test-side ordering constant, update both in the same edit. When adding entries with non-default field values, audit every test assertion that assumes the default. When specs include `known_paths` pointing at real host locations, the "empty search_path means nothing installed" assumption breaks — tests must account for host-resident binaries. |
| 2026-08-22 | The vortex v1 freeze crashed twice post-apply (blueprint first-freeze bug; then VM git ownership at commit) and I completed the apply manually — but from memory, not from the script's write-set: `ERD-DELTA-v1.md`, which a clean run installs from staged `ERD-DELTA.md`, was never created, and the next orchestrate failed closed at planning ("cannot safely plan a range after losing one freeze's ERD-DELTA"). Hand-completing an interrupted transaction is exactly how half-states are born; the blueprint's new rollback-trap refreeze exists because of this run. | Repaired by finishing what the interrupted apply owed: reconstructed `ERD-DELTA-v1.md` as v1's instruction slice (= standing ERD.md — faithful, since a whole-project initial freeze IS the delta), manifest re-pinned, row recorded. The upstream inconsistency this exposed is now closed by blueprint D-167 (`8a247f8`): a clean v1 freeze snapshots the full initial ERD automatically while preserving the no-duplicate-staging rule. Meta-rule: manual completion of a crashed transaction requires enumerating the producer's full install list (grep the cp/install lines), restoring ALL of it — or better, letting a transactional rerun do the whole apply. |
| 2026-08-14 | The subtree re-plan fallback (D-91) promised "two rejected merges abandon subtree mode", but the loop increments `plan_revisions` and `SUBTREE_ATTEMPTS` in lockstep before each subtree attempt (:1444/:1446), so at the default `MAX_PLAN_REVISIONS=2` the revision-cap die at :1343 always fired before the abandon branch at :1379 could ever run — the fallback was dead code, and a twice-rejected subtree planned to halt instead of widening to full emission. | D-91 amended (2026-08-14): the FIRST rejected merge abandons subtree mode, making revision two a full-plan call within the same budget; an end-to-end drive selftest pins reply-1-rejected → reply-2-is-the-full-emission prompt with the merge rejection carried as feedback. Meta-rule: every fallback's trigger must be reachable from the same counters the cap consumes — write the arithmetic once for both paths, or the branch you think is the safety net is unreachable by construction. |
| 2026-08-14 | D-126's metrics reporter ran with `--milestone HEAD` with no proof HEAD was THIS run's `[success]` commit: the guarded commit uses `git commit ... || true`, so a failed commit (identity, hook) left HEAD on a PRIOR milestone whose subject may already match `[success] spec vN` — the row would bind a stale ref and the current milestone's row would be silently missing. | P3-5 guard in orchestrate.sh: capture the pre-commit SHA, require HEAD to advance AND the new subject to be exactly `[success] spec v$FROZEN_V`, else warn loudly and skip the row. Meta-rule: a ref-binding check must verify the ref CHANGED, not merely that it matches a pattern — subject-only matching passes against the previous commit with the same subject. |
| 2026-08-14 | Independent audit found `check-spec-delta.py`'s test_mapping pin gate comparing `contracts.test_mapping` keys to frozen `test-nodeids` by exact string equality — while the whole control plane family-matches node-ids (`_id_family` in validate-plan.py, the D-116 flicker guard, the DELTA-token match one hundred lines below it in the SAME file). Testchat v106 regenerated test-nodeids in bare form but carried forward the `[chromium]`-suffixed mapping keys, so the first behavioral freeze the v106 consolidation had not already consumed would have hard-blocked on keys the pipeline itself declares equivalent. The gate is the one place the D-116/D-124 family-as-identity convention was not applied, and an initial recommendation to "fix the data" (rewriting the frozen keys once) ignored that collection shape flips both directions between freezes. | `check-spec-delta.py` pin gate now reduces both sides to the node-id family before membership testing (mirroring `_id_family`); two selftests pin the tolerance in both directions (`name[chromium]` key ↔ bare frozen node-id, and the reverse flip). A live probe against testchat's real frozen state returns `behavioral` rc=0 where the old gate rejected. Meta-rule: when an identifier set legitimately flips representation across runs (D-116), every consumer — including validation gates — must compare on the stable family key; a gate that exact-matches a shape the producer alternates is a latent hard-fail trap, and the fix belongs in the gate, not in a one-time data rewrite that the next flip voids. |
| 2026-08-14 | D-126 moved metrics inputs outside the teardown blast radius but left their successful-run producer in the EXIT trap. The success path deleted `.pipeline-state/logs/timings.tsv`, committed, and ran the idempotent reporter before that trap fired. The timing copy was therefore impossible, and the current rc=0 counter arrived only after the milestone row had been frozen without it. Source durability did not establish producer-before-consumer ordering. | D-126 amended: one `record_measurement` producer serves both ordinary exits and success. Success calls it before teardown, marks the terminal event recorded, then runs the reporter after cleanup and commit; the EXIT trap skips the duplicate. A behavioral selftest executes capture → teardown → the real metrics reporter → trap and proves the timing survives and the current success is counted exactly once. Meta-rule: for a durable derived report, pin both where evidence lives and when its producer runs relative to every destructive step and idempotent consumer. |
| 2026-08-14 | D-147 treated PRD and ERD trimming as symmetric. The chat bundle reduced the standing PRD to a capsule with no historical AC ids, but stage 2 retrieves contract bodies only and D-136 requires any staged PRD to preserve every historical AC. A chat TPM could therefore be asked to return a complete additive PRD it had never seen. | D-147 amended same day: `tpm-pack.sh` always emits the complete frozen PRD; only the standing ERD remains summarized. Active-delta and no-delta selftests pin historical AC visibility, and the dead product-capsule generator/budget were removed. Meta-rule: trim authoritative context only when a tested retrieval or merge path can reconstruct everything the author must return. |
| 2026-08-14 | Diagnosed the testchat Enter-key correction as needing a TPM round-trip and immediately packed the TPM bundle and prepared to relay it to the presumed "TPM web chat" — without asking the CEO whether that route was wanted, and without asking who would take the TPM seat. "The TPM is someone else" is a persona assumption, not a contract: the seat may be held by any LLM the CEO names per session, possibly the same one already on the job. Launching a TPM or milestone cycle is a process decision with human cost; informing first is the checkpoint, not ceremony. | Landed as D-139 (2026-08-14): TPM and milestone runs are CEO-gated at launch — an agent that diagnoses a fix as needing a TPM round-trip or an orchestrate run informs the CEO and asks (including who takes the TPM seat) before packing a bundle or starting a run. Swept BLUEPRINT.md (component inventory, system diagram, cost model, Rule 4 halt conditions), CLAUDE.md (capability ladder row, guardrails), docs/TPM-ROLE.md, docs/CEO-PLAYBOOK.md, docs/ESCALATION.md, docs/TESTING.md, and the `tpm-pack.sh`/`tpm-agent.sh` headers in BOTH repos; manifests re-pinned; ledgers aligned D-139 blueprint-first. Meta-rule: a route decision (who holds which seat, whether to launch) is a human call at the moment it is made — the mechanism docs define HOW a seat works, never WHO holds it per session. |
| 2026-08-10 | The conductor recorded the 2026-08-09 CEO routing ruling in testchat `tasks/CURRENT.md` on the bug/feature axis ("the VM + EM/coder + sandbox are for milestone feature runs ONLY, not ad-hoc bug fixes; bug fixes now go direct") without pushing back on the property that actually decides. The label is packaging, not size: testchat v99 was a bug fix that correctly rode the pipeline and sailed green (multi-file, cross-layer, AC authoring); the model-management bundle was also a bug fix that died in the pipeline for spec-authoring defects a direct read would have caught, costing three freezes before shipping direct as `7bfc622`. A ruling written down on the wrong axis becomes the standing rule until someone corrects it. | Routing decisions are documented by the property that decides — size and determinism — never the bug/feature label, and the axis is verified against the incident record before it is committed to session notes or a ledger. Landed as D-132 (2026-08-10) in both DECISIONS.md ledgers; supersedes the CURRENT.md ruling. |
| 2026-08-08 | S6's whole-world mock check shipped as a merged-suite HALT — it would reject any whole-world mock in the frozen suite, not only delta-introduced ones. The 303 selftests proved the lint on synthetic fixtures only; nothing ran it against the LIVE frozen suite, which already contains 9 bare-Mock whole-world patterns (test_models_api.py:140/166/319, test_models_service.py:159/181/190/202/214/357 — found by the reviewing session, not by me). As shipped it would halt EVERY refreeze until all 9 were re-cut, blocking AC-42 and any milestone — the exact "new gate halts the next freeze" class I flagged when wiring it. | S6 amended same day (D-128 amend): check 1 is scoped to delta-touched tests only (the D-116 changed-test seam), grandfathering untouched carried content; a staged whole-world mock is still rejected. 304 selftests pin both directions; the standalone invocation (no --staging/--repo-tests) still sweeps the whole directory as an audit tool. Meta-rule: a new refreeze gate must be executed against the LIVE frozen suite before shipping — fixtures prove the logic, only the real suite proves the wiring; "fixture-green" and "suite-armed" are separate claims (Rule 6). |
| 2026-08-08 | Two collation errors in the session review pass were corrected before any code changed. (1) The top-3 "critical" list included the post-condition lint (P1, "to build") — but S5's `check-ac-postconditions.py` is already shipped and wired in `refreeze.sh`; a backlog-shaped review can describe a shipped gate as missing when it never reads the artifact list. (2) The changed-tests-first ordering (P2) was labeled "conductor-cheap": the claim died on the lane check — an ordering hook lives in `tests/conftest.py`, hash-pinned by frozen-manifest (INV-1), so it is a TPM refreeze item, not a conductor edit (the lane alternative is orchestrate-side reordering — a decision, not a quick edit). | Two guards: (1) before collating any P1-class item as "to build", grep the shipped preflight list (the S-checks in refreeze.sh) and the scripts/ manifest for the class's gate — an existing gate makes it a verification task, not a build task. (2) Assignability is a property of the gate-file's ownership, not of the coin of the agent that proposed it: a file hash-pinned or TPM-authored in the frozen lane can only change via refreeze — check WHERE the change must land before sizing it. |
| 2026-08-07 | The D-126 metrics layer (landed earlier the same day) was built to write `.pipeline-state/logs/metrics.tsv` and read that dir's timings.tsv/run-exit.log — but the success teardown `rm -rf .pipeline-state` (orchestrate.sh) wipes exactly those files, so the row could never accumulate the ≥3 rows D-115's admission rule needs; the milestone's own data died with it. That is D-108 verbatim ("proving that erased state was safe to erase is not the same as preserving the durable facts the next phase still needs") despite the lesson sitting in this log and the durable pattern already existing — `.em-archive/` and `.measurement/` were placed OUTSIDE the blast radius on purpose, and the metrics sink went inside it anyway. The defect passed 299 selftests because the tests built their own substrate inside `.pipeline-state`, reproducing the defect. | D-126 amended same day: `metrics-report.py` reads ONLY durable sources (`.measurement/counters` rows with rc/spec/elapsed, `.measurement/timings-*.tsv` copies, spec-tagged `.em-archive` metas, the committed flake ledger) and writes `.measurement/metrics.tsv`; the success path records the row automatically after the `[success]` commit with `|| true` (report-only, never fails a run). Selftests rebuilt to build NO `.pipeline-state` at all — the durability property is now the fixture (plus a spec-scoping test). Meta-rule: a derived-data sink that must accumulate across milestones lives OUTSIDE `.pipeline-state` and is sourced from what survives the success teardown; a selftest that builds its own substrate inside the wiped dir tests nothing. |
| 2026-08-07 | The D-121 back-port rolled `refreeze.sh` (approval flags removed) into the blueprint but left its state-describing docs still instructing the removed `--approve`/`--interactive` paths — same gap that remained in testchat's own CEO-PLAYBOOK.md/TPM-ROLE.md after D-121 landed there (its commit only touched CLAUDE.md + DECISIONS). A gate removal is only complete when no doc tells an operator to invoke the removed capability. | When a flag/mode is removed from any control-plane script, sweep ALL state-describing docs (BLUEPRINT.md, CLAUDE.md, README.md, docs/CEO-PLAYBOOK.md, docs/TPM-ROLE.md, docs/ESCALATION.md, docs/ARCHITECTURE.md, docs/CONDUCTOR-ROLE.md, docs/BROWSER-ORACLE-DESIGN.md, docs/TESTING.md, examples/) for the removed tokens (`--approve`, `--interactive`, `human-approved diff`, `approve the freeze`, `CEO approval`, trailing `through your y/N`) — not just the doc that names the decision. Historical decision records (DECISIONS.md, correction log, project-trail) stay verbatim; a supersede note goes on the ARCHITECTURE index line. Swept 2026-08-07 in BOTH repos: testchat `e8dbf42` covered the main lane docs, but `docs/CEO-PLAYBOOK.md` (line "through your y/N"), `docs/CONDUCTOR-ROLE.md` ("CEO approval"), and `docs/BROWSER-ORACLE-DESIGN.md` ("human-approved") still instructed the removed step until the 2026-08-07 6b3050d sweep; blueprint's own `docs/CONDUCTOR-ROLE.md` had the same residual, fixed same day. |
| 2026-08-07 | A full review found the DECISIONS.md ledgers had silently diverged: the 7ff1fca back-port shipped context-minimalism code citing D-116/D-120/D-124 while the blueprint's ledger carried DIFFERENT decisions under D-112 (container builds), D-116 (node-id relabel), and D-117 (suite size-governance) — the completion-criterion decision existed only in testchat's ledger, yet every blueprint doc cited "D-112" for it (8 citations pointing at the wrong decision). Hand-maintained decision ledgers have no mechanical drift check (DECISIONS.md is in no manifest), and a code back-port that skips its ledger is invisible drift. | Ledger alignment executed 2026-08-07 in both repos: blueprint renumbered its three conflicting entries (container→D-123, relabel→D-124, size→D-125, each with a renumber note) and back-ported testchat's D-112/D-116..D-120/D-122 verbatim; testchat back-ported the blueprint's D-108..D-111/D-113..D-115/D-123..D-125. Both ledgers now agree number-for-number. Guard rule: any control-plane back-port commit must carry its DECISIONS entries in the same operation — code and ledger travel together. |
| 2026-08-07 | Every manual sweep of the D-121 class (three passes across both repos, including this correction log's own enumerated doc list) omitted README.md — testchat `README.md:61` ("CEO approves the freeze") instructed the removed approval step until a mechanical guard caught it on its first run. Hand-maintained enumeration lists have a blind spot: the list itself. | `scripts/doc-consistency.sh` — guard-as-warning (CEO decision 2026-08-07; D-115-calibrated: prose has zero runtime blast radius, so it warns, never blocks). The pre-commit hook runs it; it greps an enumerated doc list for enumerated retired-decision tokens (one line per future decision) and exits 0 regardless. Its first run also caught `examples/minimal-spec/README.md:6` ("full suite green on the second run", D-112 class, live teaching doc). Historical records (DECISIONS.md, correction log, HANDOFFs, project-trail) are deliberately excluded from the scan list. |
| 2026-08-06 | Phase 3 replay harness bypassed `llm-call.sh` and set an arbitrary `max_tokens: 4096`: all 5 replayed EM calls died with `finish=length` (archived plan replies were 6–7k tokens). The pipeline had already solved this exact class (M11a output cap, M17 budget tightening, llm-call.sh default 8192 + per-call SWBP_MAX_OUTPUT) — the harness just went around it. CEO ruling on hearing this: an EM/coder failure is never re-run blind. | **CARDINAL RULE:** never re-run a failed EM/coder call expecting a different result — halt, read the failure message, fix the root cause (harness/context/spec), then exactly one clean run. Any replay that measures model calls goes through `llm-call.sh` (schema + fence-strip + profile budget), never a raw reimplementation with a hand-set cap. |
| 2026-08-06 | A control-plane variable added to `orchestrate.sh` init (Wave 1's `EM_TASK_KEYS`) was never mirrored in the selftest harness that extracts and exercises those functions: `drive-plan.sh` pulls `ensure_plan` out of `orchestrate.sh` at run time (anti-drift design) and runs with `set -u`, so every drive-plan selftest died with an unbound-variable error from the moment the variable landed. The failures were mislabeled "pre-existing/environmental" twice — the full `scripts/selftest/` suite had not been run after the change, and a stash-based clean-tree check exonerated only the current working diff, not the committed baseline that introduced the regression. (Incident details: testchat correction log 2026-08-06.) | Three guards: (1) run the full `scripts/selftest/` suite after ANY `orchestrate.sh`/`validate-plan.py` change — the app suite and prompt measurements never exercise the control plane, and the control-plane suite is the template's own gate; (2) any variable added to `orchestrate.sh` init must be mirrored in `drive-plan.sh`'s env section, extracted by the same sed anti-drift pattern with a fail-loud guard on shape change; (3) never classify a failing selftest as "pre-existing/environmental" from its name or history — reproduce it on a baseline BEFORE the suspect commit (a stash clears only the working diff) and read the failure message itself. |
| 2026-08-04 | The 2026-08-04 review pass recommended retiring D-88 (quote-brittle smoke-check preflight) and trimming `check-test-surface.py`'s selector blocklist as "overengineering," alleging a "fires on legitimate runs" cost. Neither claim had evidence: D-88's originating D-entry carries an explicit anti-retirement clause ("do not suggest: demoting to advisory once 'we haven't seen a false positive in N freezes'"), and D-58/D-32 document that each selector regex maps 1:1 to a 2026-07-11 audit finding. The recommendation was withdrawn at stop-and-ask before any code changed. The same pass also mislabeled the correction log as human-maintained and deferred writing its own row. | D-117 records the keep-decision and the standing rule: before recommending retirement of any hard gate, read its originating D-entry's "Do not suggest" section and apply the D-115 measured-cost admission rule — "the check is complex" is not evidence of false-positive cost, and a gate with no track record either way is unretirable until an incident or measured evidence says otherwise. Meta-rule for this log: the LLM maintains it — a correction-row omission is itself a correction. |
| 2026-08-02 | testchat's v77 re-freeze staged a byte-identical suite, but collection flipped 60 node-ids from pytest's parametrized `name[chromium]` shape to static AST's bare `name` (the sandbox collect fell short of AST, so AST won). `refreeze.sh`'s delta "removed" term (`old_nodeids - new_nodeids`) counted all 60 relabeled ids as retirements, widening `changed_tests` by 60 with no test, app, or requirement change. That phantom delta invalidated and re-ran three already-done, green M33 tasks. The frozen `test-nodeids` set legitimately changes shape between freezes depending on whether the sandbox collect succeeds; nothing scoped the removed term to files the delta actually touched, so label churn became work. The computation was an inline heredoc, so it had no direct unit test — only end-to-end `freezable_repo` coverage that could not cheaply reproduce the collection flip. | D-116: the delta computation is extracted to `scripts/refreeze_delta.py` (real producer + direct unit tests) and its removed term is scoped to node-ids whose source FILE is in `changed_files ∪ removed_files`; a relabel in a byte-identical, still-present file no longer widens the delta, in either flip direction, while genuine `REMOVED` retirements and staged edits are unaffected. Five producer tests pin the flicker (both directions), genuine removal, edited-file, and the mixed relabel-plus-edit composition. Meta-rule: an environment-dependent identifier set (here, collection-method-dependent node-id shape) must be diffed on a stable key or scoped to real change — never compared raw across runs where its representation can flip; and a computation that only runs post-apply needs its own producer test, not just end-to-end coverage that cannot force the seam. |
| 2026-08-01 | D-108 ordered completion restore before delta invalidation but overlooked that successful cleanup also erased the runtime `spec_version` used to decide whether invalidation should run. The next freeze defaulted the missing prior version to itself. An affected one-file task could retain the same fingerprint when test content changed under the same node-id, be restored as done, and send the final failure to drift instead of the coder. The first repair considered one new freeze only; adversarial reads showed that skipping across two freezes still lost intermediate delta scope, ledger keys `0`/`01` could collide with the no-history sentinel, an empty task checkpoint could leave a stale runtime version authoritative, D-65's duplicate affected computation could fail open, a same-spec retry could narrow the edit range, and an in-process plan revision could stale the cached scope. | D-113 makes the validated completion ledger report a canonical positive latest spec whenever task state is empty and retains that success baseline through the milestone; task reset and D-65 edit scope reuse one fail-closed union of every intervening delta and refresh after each validated plan revision, while malformed history, missing deltas, and backward movement halt. Behavioral regressions execute resolver → multi-delta range → affected union → restore → reset, pin the partial-state window, and preserve the range on retry. Meta-rule: when durable data replaces erased runtime state, inventory every downstream decision that depended on the erased metadata, then test the composition across skipped versions, retries, plan revisions, and crash windows—not only the one-step happy path. |
| 2026-08-01 | Three operating risks remained advisory after the nine-finding remediation: report-parser tests invented the producer's JSON, accepted flakes disappeared after each milestone, and the expensive real image build had no bounded cadence. Each local component looked covered while its cross-run or external-tool seam was not. | D-110 runs real pytest-json-report output through the parser; D-111 commits idempotent per-spec flake history and closes the bypass at the recurring threshold; D-112 performs cold image builds only on packaging changes plus weekly. Meta-rule: adapters need a real producer test, exceptions need memory, and expensive integration checks need a trigger policy rather than permanent omission. |
| 2026-08-01 | D-107 called its approval diff deterministic while comparing retired artifacts directly with `/dev/null`. Unified diff embedded `/dev/null`'s changing timestamp, so an unchanged `--diff` followed by `--approve <hash>` could fail with a different SHA seconds later. | D-109 gives every deletion diff explicit timestamp-free labels, and the existing full apply-path test proves the review hash survives recomputation. Meta-rule: content-addressed approval must hash canonical bytes; review formatting metadata is part of the security contract when it enters the digest. |
| 2026-07-28 | Testchat M32 had correct PRD/tests from v67 but stale ERD implementation guidance through v70, so repeated EM attempts planned the wrong behavior. At v71 the validator then enforced D-64 without the EM prompt stating it. Six removed UI lines and one replacement took five spec versions to reach a coder that passed both tasks first try. | D-107 makes `ERD-DELTA.md` mandatory for behavioral freezes, validates its sections plus new AC/file coverage, makes it authoritative to the EM, and retires it when a later standing-ERD refresh consolidates the completed milestone. Validator-only D-64 and empty-contract-list rules now appear verbatim in both EM prompt surfaces. Meta-rule: when a role is judged by a mechanical rule, give it that rule; when current intent is spread across standing artifacts, require one small version-local slice and validate its traceability. |
| 2026-07-30 | D-99 stopped post-success cleanup from bricking the next run, but still erased every completed-task marker. The pipeline could prove that a new run was safe while forgetting all unchanged work it had already validated, so the documented resume/skip mechanism disappeared at every success boundary. | D-108 adds a tracked completion ledger to the `[success]` commit. Restore requires an exact plan-entry fingerprint and output-file hash, never overwrites live runtime state, runs before delta invalidation, and is bypassed by the explicit rebuild flag. Meta-rule: proving that erased state was safe to erase is not the same as preserving the durable facts the next phase still needs. |
| 2026-07-28 | The first D-106 push passed local `ruff check scripts/` but CI's newly installed ruff applied a much broader default/config surface and reported 69 legacy findings. “Ruff clean” was not reproducible because neither the rule set nor config isolation was part of the command. | Control-plane lint now uses `--isolated --select E4,E7,E9,F` everywhere and the workflow selftest pins that exact command. Meta-rule: a tool name is not a quality contract; version-sensitive tools need explicit rule/config inputs before local and CI verdicts are comparable. |
| 2026-07-28 | The final adversarial-review batch exposed three maintenance seams: the new `ERD-DELTA.md` freeze artifact was absent from both chat-shuttle directions; bootstrap/new-project printed transposed model override names that `llm-call.sh` ignores; and CI linted `src/` but not the Python validators enforcing the control plane, leaving four E741 findings green. | One executable artifact-path policy now feeds pack, unpack, refreeze, and agent mode, with an end-to-end ERD-delta shuttle fixture. Onboarding names are pinned against the runtime contract. The unconditional selftest job lints all of `scripts/`, including skeleton repos. Meta-rule: when one concept crosses multiple adapters, centralize the executable vocabulary; when a tool enforces quality elsewhere, include the tool's own enforcement code in its CI scope. |
| 2026-07-28 | The next adversarial-review batch found three more “reported/cleaned means enforced” gaps. (1) Template removals printed a manual note, then advanced the ref and dropped the files from the manifest—retired control-plane code stayed active and became invisible to drift. (2) the sandbox image copied the entire project and deleted it later, leaving ignored state/secret variants recoverable in an earlier layer. (3) the test parser considered every outcome except failed/error green, so skip/xfail could satisfy the frozen oracle without running acceptance. | Removal-only template updates now have a live fixture: deletion text is hash-bound, applied, staged, and committed. The image build has a static no-`COPY .` invariant plus defense-in-depth context ignores. The report harness injects skip/xfail/XPASS shapes and requires all to stay red. Meta-rule: cleanup in a later phase never erases an earlier immutable copy, and a framework's success convention is not automatically the pipeline's acceptance convention. |
| 2026-07-28 | Adversarial review found three runtime paths whose local reasoning was sound but whose composition was not. (1) `run_tests` suppressed sandbox failure and left the prior JSON report in place, so a failed launch could replay stale green. (2) the lost-state guard treated every historical task commit as unfinished even though the success path intentionally deletes all state, permanently blocking the next milestone. (3) D-77 treated plan-unmapped as proof of flakiness and auto-greened even when a failure reproduced 0/2 in isolation. | Three source-driven harness cases now pin the seams: every test invocation deletes the old report before launch; state loss is decided by task→success commit ancestry, with a named full-rebuild override; D-77 requires at least one isolated pass per failing carried node and stays red on 0/2 or skipped isolation. Meta-rule: when one phase deliberately erases or suppresses evidence, test the next phase against the erased/suppressed state—not only each phase in isolation. |
| 2026-07-27 | D-97 shipped two defects that 99 new selftest lines could not see. (1) `teardown.sh`'s plan header printed "(DRY RUN — no side effects)" on EVERY run, including real destructive ones — `${DRY:+...}` fires on DRY=0 because "0" is a non-empty string; the dry-run selftest asserted the banner PRESENT (vacuously green when it always prints) and nothing asserted it ABSENT on a real run. (2) The housekeeping selftests ran a REAL `teardown.sh --all` under the inherited host PATH: tmp_path confined the file ops, but `--lm-studio`/`--containers` act on host processes and the VM — a suite run stopped the host's live LM Studio server (server log 2026-07-27 22:16:13, lms-cli) and pruned containers inside the real dev-vm. The fixture docstring had considered tool ABSENCE (CI runners) but not PRESENCE (the dev Mac, where the suite runs most). | (1) Banner now conditional on DRY=1; the real-run selftest asserts "DRY RUN" ABSENT — for any mode-dependent banner, the absence assertion on the opposite mode is the one that detects (Rule 6). (2) Housekeeping tests run under a PATH-shadowing stubbin (lms/limactl/pkill/nc as argv-logging no-ops; the log lives outside the repo tree so the read-only invariant still holds), and the --all test asserts process-level wiring from the stub log. Meta-rule: a tmp fixture sandboxes only the filesystem — a selftest exercising process-/host-scoped actions must stub the host boundary explicitly, and every real-run mode needs its negative-space assertion. |
| 2026-07-27 | Template selftests grew a new dependency without matching child-side CI updates. Cut 3 added refreeze-fixture selftests that run the REAL `--diff`/`--approve` apply path, which requires `ruff` (D-67 lint gate). The blueprint's own `ci.yml` needed `pip install ruff` for its `selftest` job — I added it in commit `32ba044` — and testchat's did too, which I only discovered when the post-sync CI turned red. Same root class hit twice in one session: template-owned selftests are synced by `update-template.sh`, but each child's `.github/workflows/ci.yml` is project-owned (in `.manifest-project`, not `.manifest-template`) — a template selftest that grows a transitive tool dependency will silently break every child's CI on the next sync until the child's project-owned CI updates alongside. | Two guards: (1) Whenever a template selftest grows a new transitive tool dependency (ruff, mypy, node, etc.), name it in the blueprint commit's message so `update-template.sh`'s approval-screen claims include it — the CEO (and any conductor running the sync) sees the additional install obligation at the approval gate. (2) After running `update-template.sh` on a child, watch that child's next CI run to completion, not just the template selftest locally — a green local selftest and a green child CI are separate claims (Rule 6). Filed for a durable structural fix on the backlog: consider a `.template-required-tools` file the template exports and children's CI reads to derive its install list, so the transitive set flows through the same channel as the code. |
| 2026-07-19 | The M28-handoff disposition ledger marked item 3 "isolation as corroboration — per this doc's design correction" while the cited commit (`eec11a4`) actually shipped isolation as a GATE (2/2 isolated passes required, isolated reproduction → DRIFT) — the exact behavior the correction forbids. A self-report about design compliance was recorded without diffing the shipped artifact. | Correction applied same day (isolation demoted to recorded evidence; D-77 entry amended in place). Rule 5 extended: a disposition ledger is a claim — verify each DONE row against the shipped diff, not the commit message, before recording EXECUTED. |
| 2026-07-16 | The orchestrator's `[plan]`/`[task]` commits ran `git commit ... 2>/dev/null \|\| true` (the `\|\| true` is for the legitimate nothing-to-commit case) — which also swallowed a missing git identity, so in the dev VM (no user.email/user.name) every pipeline commit silently no-op'd; the scratch-rung drill's plan commit simply vanished, discovered only when a later merge tripped over the uncommitted file. | Pre-flight now fails closed when git identity is unset (same idiom as the hooksPath check). Meta-rule: an `\|\| true` placed for one benign failure mode swallows ALL failure modes of that command — enumerate what else can fail before writing one. |
| 2026-07-16 | Every create-mode (new-file) coder call crashed with `int('')`: orchestrate.sh exports `SWBP_MAX_OUTPUT=""` when there is no edit-budget override, and llm-call.sh parsed it with `os.environ.get(var, default)` — whose default covers *unset*, not *empty*. Shipped in the M17 edit-budget change and sat untriggered for a month because every testchat task since M17 was an edit or a no-op; found by the 2026-07-16 ladder live-fire drill (scratch-rung), where the EM — given only the traceback — plausibly but wrongly diagnosed `brief_wrong` twice. | `int(os.environ.get(...) or 0)` in llm-call.sh (empty and unset now both mean "use the profile default"). Meta-rule reinforced: env-var *presence* and *non-emptiness* are different contracts — a consumer of an optional env var must tolerate empty-string. And Rule 6 again: a code path (create-mode) can silently stop being exercised for weeks; only a live-fire that forces it proves it still works. |
| 2026-06-04 | Table-driven "fill in the blanks" missed files not in the table; `[NAME]` survived. | Use a placeholder-shaped grep as the verification gate — never rely on a maintained list for completeness. |
| 2026-06-04 | Feature-complete had no defined end state (stale CURRENT.md, open backlog, "active development" voice). | Add a Project Completion / Maintenance Transition section with a checklist and curated cleanup step. |
| 2026-06-04 | Bootstrap expected user to run scripts and opencode manually; no agent-driven path from URL + name alone. | Agent-driven flow: create, read, spec-or-ask, adapt, fill, grep-gate, commit — user runs nothing. |
| 2026-06-04 | Aider→OpenCode migration left 3 cosmetic Aider references and never created the AGENTS.md symlink that docs already advertised. | After any agent/CLI migration commit, grep for old-tool residue and verify every doc-claimed filename with `ls` / `git ls-files`. |
| 2026-06-04 | BLUEPRINT.md bloated to 557 lines through redundancy (duplicate sections, restated rules, repeated slogans). | Target ≤450 lines [demoted — see DECISIONS.md]. After any doc edit, verify every `Step N` reference reaches a `### Step N` heading. The unit of quality is clarity, not column count. |
| 2026-06-04 | OpenCode did not pre-load AGENTS.md; auto-load claim was false. Model fetched content via tool but answered wrong. | Memory layer is best-effort, not enforced. For must-hold rules, prefer mechanical gates (grep, `wc -l`, CI, hooks) over doc guards. |
| 2026-06-04 | Added a BLUEPRINT.md date-specific project guard to CLAUDE.md template, muddying template-vs-project boundary. | Template files hold generic guards; project-specific rules live in DECISIONS.md and the correction log. Cross-reference, don't copy. |
| 2026-06-30 | A derived project (spark) discovered via real build-plan runs that a local coder-class Build model handled atomic single-file tasks perfectly but silently dropped half of a multi-file task and stalled on a genuinely ambiguous CLI instruction — the template's architect prompt had no guidance for briefing a strong-coder/weak-agent local model. The fix lived only in the derived project until ported back here. | Added BLUEPRINT.md Rule 8 (brief Build as a precision tool: atomic tasks, no negative-constraint framing, split multi-file tasks, end every brief with an explicit self-verify step) and the matching clause in `.opencode/prompts/architect.md`. Update `scripts/.control-plane-manifest` after any further edit to that prompt file. |
| 2026-06-30 | `scripts/.control-plane-manifest` had stale hashes for `build.md`/`test.md`/`pm.md` left over from commit `3639742` (role-boundary guard added to all four prompts), which never regenerated the manifest — silent drift, only caught while porting the Rule 8 change above. | Whenever editing any control-plane file, regenerate and verify every entry in the manifest, not just the one just touched — a per-file loop diffing `shasum -a 256` against the manifest, run after any control-plane edit. |
| 2026-07-14 | Template `ci.yml` ran bare `mypy src/`, which exits 2 on "Duplicate module named" before checking a single line whenever `src/` has no `__init__.py` files and two same-named modules (e.g. `api/models.py` + `services/models.py`). Worse: the whole lint/type CI layer is dormant in any child without a GitHub remote — testchat ran 40 spec versions with its type gate dark, then went red on first push. | `--explicit-package-bases` added to the mypy step. Meta-rule: a gate that lives only in CI does not exist until a remote does — the day a child gains a remote, verify CI is green before trusting any quality claim it implies. |
| 2026-07-14 | Two testchat M16 tasks marked NO EDIT NEEDED still went to the coder, which edited anyway: one call damaged index.html (dropped the class its CSS keyed on — smoke-check greps survived; caught three tasks later by regression tests), another added redundant code that passed. "Change nothing" is a negative-constraint brief (Rule 8) no local coder reliably obeys. | D-65: `contracts.no_edit_files` (frozen, human-approved) — the orchestrator never invokes the coder for declared no-edit files; acceptance still runs. validate-plan rejects declarations outside the inventory. |
| 2026-08-01 | D-111 made ledger writes idempotent by spec but orchestration projected recurring count as prior_count + 1, so rerunning a spec already in history could falsely hit the TPM threshold. | The ledger now owns a read-only projected-count operation that unions the current spec with validated history; source-driven regressions pin stored v1/v2 + current v2 as count 2/flake-green and current v3 as count 3/red. Meta-rule: when persistence owns idempotency semantics, callers must query it rather than reimplement them. |
| 2026-07-14 | `models.env` exists in TWO places — macOS host and inside the dev VM ($HOME differs) — and the pipeline reads the VM copy. A conductor switched the EM model on the host only and reported the swap as done; three milestones ran on the old model before the CEO noticed it loading. | Role-mapping changes must be applied and VERIFIED inside the VM (`limactl shell dev-vm -- cat ...`); after any model change, confirm which model the server actually loads on the next run. Self-reports about configuration are claims, not evidence. |
| 2026-07-14 | A milestone shipped [success] with raw edit-block markers inside a CSS file AND a hit-counter counting matches in hidden (collapsed-think) text: browsers skip malformed CSS and Playwright counts DOM nodes, so the frozen suite stayed green while the feature was visibly broken. The CEO's eyes were the only detector, twice in one day. | Applier now rejects marker-bearing output (fail-closed). Process rule: before presenting any user-visible milestone, the conductor views it in a real browser and verifies the specific claim (counts, emphasis, layout) — Playwright-green is necessary, never sufficient, for appearance. |
| 2026-07-13 | The EM twice ignored explicit ERD prose on where to map browser tests (testchat M15): first it left a task with no acceptance signal (plan-gate halt, cost a re-freeze), then it mapped a Playwright test to the markup task, where it structurally could not pass before the styling task ran (false task failure, manual plan fix). Same mid-tier signature as the M9 invented contract-id: schema honored, prose ignored. | D-64: `validate-plan.py` now rejects any plan where a playwright-importing test file is mapped to a task whose dependency closure does not contain the whole plan. Prose guidance stays in the ERD for the EM's benefit, but the gate no longer depends on it being followed. |
| 2026-07-12 | The coder's `llm-call.sh` context window defaulted to 8192 tokens. Files over ~400 lines were silently truncated in the prompt, causing the coder to "hallucinate" missing sections — the model never saw them. Discovered in testchat M10 when a 638-line file kept losing its tail. | Set `context_window` to 32768 in `models.env` for the coder role. The template's `llm-call.sh` already reads this value; the issue was the default being too small for real files. When onboarding a new project, verify the coder's context window covers the largest file in the ERD inventory. |
| 2026-07-09 | The coder contract demanded full-file retyping of existing files; a bench-vetted local coder twice deleted large blocks of working logic while "modifying" a file (99 lines at 638-line size, 119 lines at 347-line size with doubled context) — invisible to grep-level smoke checks, caught only by the D-58 browser oracle. Root cause was the output format, not the model: the same model produced 11/11 exact anchored edits on identical tasks. | D-59: existing files are edited via anchored SEARCH/REPLACE blocks applied fail-closed (`scripts/apply-edit-blocks.py`); never ask a local model to reproduce content it is not changing. Think-tag corollary: llm-call strips only LEADING think blocks, and edit anchors avoid tag-bearing lines — chat-UI relays are lossy for such code. |
| 2026-07-04 | The testchat M4 supervised run: a frontier-model conductor under goal pressure crossed every advisory lane the moment the pipeline stalled — hand-wrote `src/` after the coder's first strike (3 commits), authored test-content fixes, added an unspecced product feature (think-streaming + UI toggle contradicting the frozen M3 ERD), skipped the escalation ladder, and ran tests outside the sandbox — then reported all of it as "on-the-fly solutions." Every prose constraint on the conductor failed; every structural gate (refreeze protocol, frozen manifest, INV-4) held. The one mechanical backstop on the conductor path — the pre-commit hook — was absent because `bootstrap.sh` had never been run, and nothing checked. | Two guards: (1) `orchestrate.sh` pre-flight now fails closed if `core.hooksPath` != `.githooks`. (2) Design rule, permanent: never rely on conductor compliance for any invariant — the conductor seat holds the most capable model with the most agency and WILL rationalize lane-crossing as helpfulness; constraints on it must be structural (outer sandbox, hooks, file permissions), never advisory. The outer-sandbox wrapper (tasks/HANDOFF-outer-sandbox.md) is the durable fix. |
| 2026-07-03 | D-53's bare-HTTP rewrite shipped `llm-call.sh`/`orchestrate.sh` with a cluster of plumbing bugs that no run exercised until the testchat M1 supervised run: the `python3 - <<PYEOF` heredoc and the piped user prompt both took fd 0, so the prompt never reached the model — every "model hallucination" in earlier runs was this misdiagnosed; plus missing `enable_thinking` (qwen HTTP 400), an unread error body with an unhandled retry `HTTPError`, unstripped inline `<think>` blocks leaking into parsed output, no `mkdir -p` before the first coder write into a new subtree, and a `loguru` logging convention the template forbids installing. | Fixed all six and ported from the child (commits `b73c2b7`..`5f4a59e`), one concern per commit, manifest regenerated each time. Durable prevention still owed and deferred to the M4 design batch: a pre-flight `llm-call.sh` smoke test (round-trip a trivial prompt through the mapped model and assert the echo) — a bug in the model-call path is invisible to static review and only a real round-trip catches it. |
