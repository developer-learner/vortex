# .template-version — fleet identity (D-33): the template this project was born from.
# bootstrap.sh stamps ref= at instantiation (the template's HEAD SHA at birth);
# scripts/update-template.sh --stamp retrofits a child created without it.
# In the template repository itself, ref stays UNSTAMPED and drift checks skip.
repo=developer-learner/sw-dev-blueprint
ref=bd0231ecc3b7e026fd3d1b96ae087497c5d4c7f7
